const RAW_TEXT_ID = 'raw-text-to-html-ssc-id';
const inputHtml = '<textarea id="raw-text-input" placeholder="Enter text here"></textarea>';
const TEXT_HTML_UI = new ShortCutCointainer(RAW_TEXT_ID, ['t', 'h'], inputHtml, {open: true});
